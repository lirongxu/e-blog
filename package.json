{
  "name": "e-blog",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute e-blog-db --local --file=schema.sql",
    "db:init:remote": "wrangler d1 execute e-blog-db --remote --file=schema.sql"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "marked": "^15.0.0",
    "highlight.js": "^11.11.0",
    "katex": "^0.16.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250508.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.14.0",
    "@types/katex": "^0.16.0"
  }
}
